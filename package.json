{
  "name": "yunkapuzi-miniprogram",
  "version": "0.1.0",
  "private": true,
  "description": "云咖铺子微信原生小程序 MVP",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "verify": "node --test tests/*.test.js && node scripts/check-project.js"
  }
}
